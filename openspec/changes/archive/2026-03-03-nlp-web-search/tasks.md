## Tasks

- [x] Update `src/nlp.rs` API structs: Change `ApiRequest` to include an optional `tools` field (`Option<Vec<ToolDef>>`). Add `ToolDef` struct with `name`, `description`, `input_schema` fields. Change `ContentBlock` to support both `text` and `tool_use` types (add `type_field`, `id`, `name`, `input` fields). Add `ToolResultContent` struct for sending tool results back. Change `ApiMessage.content` from `String` to an enum or use `serde_json::Value` to support both text and structured tool_result content.
- [x] Add `fetch_url` function in `src/nlp.rs`: Use `reqwest::blocking::Client` with a 10-second timeout and a reasonable User-Agent header to GET the URL. Strip `<script>` and `<style>` blocks, then extract text from HTML by removing remaining tags. Truncate to 4000 characters with a truncation note. Return `Result<String, String>`.
- [x] Add tool-use loop to `call_claude_api` in `src/nlp.rs`: After getting the API response, check if any `ContentBlock` has `type: "tool_use"`. If so, execute `fetch_url` with the provided URL input, build a `tool_result` message, append it to the messages, and re-send. Loop up to 3 iterations. Extract the final text block from the last response. If no text block after max iterations, return an error.
- [x] Add `fetch_url` tool definition to the API request in `src/nlp.rs`: In `call_claude_api` (or `interpret`), construct the `ToolDef` for `fetch_url` with description "Fetch a web page and return its text content. Use when the user asks about a URL or wants content summarized." and input_schema `{"type":"object","properties":{"url":{"type":"string","description":"The URL to fetch"}},"required":["url"]}`. Always include it in the request.
- [x] Update system prompt in `src/nlp.rs`: Add a paragraph to `build_system_prompt` telling the model it has access to a `fetch_url` tool that can read web pages, and to use it when the user mentions a URL, asks to summarize a link, or needs information from a web page referenced in a task.
- [x] Update TUI status message in `src/tui.rs`: Before calling `nlp::interpret` (~line 1084), set `app.status_message` to `Some("Thinking...")`. The existing flow already handles replacing this with the result. This provides feedback during potentially longer tool-use round-trips.
- [x] Add tests in `src/nlp.rs`: Test `fetch_url` with a mock server returning HTML (verify text extraction and truncation). Test that `parse_response` still works for all existing action types. Test tool-use response parsing (ContentBlock with type "tool_use").
- [x] Build and run `cargo test` to verify everything compiles and passes.
