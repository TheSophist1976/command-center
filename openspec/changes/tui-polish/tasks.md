## Tasks

- [x] Add NLP loading state to `App` struct in `src/tui.rs` (~line 322): Add `nlp_pending: Option<std::sync::mpsc::Receiver<Result<(NlpAction, String), String>>>` to hold the channel receiver while waiting for a background NLP call. Add `nlp_spinner_frame: u8` to track animation state. Initialize both in `App::new` (None and 0).
- [x] Move NLP call to background thread in `handle_nlp_chat` (~line 1087): Instead of calling `nlp::interpret` directly, clone the needed data (tasks, nlp_messages, api_key), create an `mpsc::channel`, spawn `std::thread::spawn` that calls `nlp::interpret` and sends the result, store the receiver in `app.nlp_pending`. Set `app.mode` to remain in `NlpChat` and return (don't process the result here).
- [x] Add NLP result polling to `event_loop` (~line 430): After each draw and before/after the event poll, check if `app.nlp_pending` has a result (use `receiver.try_recv()`). If `Ok(result)`, take the receiver out of `app.nlp_pending`, clear the spinner, and process the NLP result using the existing match arms (extract the result-processing logic into a helper function). Also advance `app.nlp_spinner_frame` on each loop iteration when `nlp_pending.is_some()`.
- [x] Update status message rendering for animation: When `app.nlp_pending.is_some()`, set `app.status_message` based on `nlp_spinner_frame % 4`: frame 0 = "Thinking", 1 = "Thinking.", 2 = "Thinking..", 3 = "Thinking...". This happens in the event loop before each draw.
- [x] Block NLP input while pending: In `handle_nlp_chat`, if `app.nlp_pending.is_some()`, ignore Enter key presses (don't submit a new query while one is in progress). Still allow Esc to cancel (set `nlp_pending = None` to drop the receiver).
- [x] Verify overdue date comparison: Confirm the overdue check at ~line 1639 uses `d < Local::now().date_naive()` (strict less-than, excluding today). If it already does, add a comment clarifying the intent. Also verify `View::matches` at ~line 104 is consistent.
- [x] Add tests: Test that `nlp_spinner_frame` cycles correctly. Test that tasks due today are NOT marked overdue in view matching.
- [x] Build and run `cargo test` to verify everything compiles and passes.
