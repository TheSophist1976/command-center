## Tasks

- [x] Add `format_action_summary` helper that formats an `NlpAction` into a human-readable summary string
- [x] Add `format_update_preview` helper that generates per-task before→after change lines (truncated at 10)
- [x] Show action summary in chat for Filter actions before applying the filter
- [x] Show action summary in chat for Update actions before entering confirmation
- [x] Show per-task change preview in chat when entering ConfirmingNlp mode
- [x] Verify all existing TUI tests pass
